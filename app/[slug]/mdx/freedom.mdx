import Link from "next/link";
import Image from "next/image";

export const images = ["f1","f2","f0","f5","f6","f7","f8","f10","f11","f12","f13","f14","f15","f16","f17"];

<div className="bg-black p-1 rounded-2xl shadow-lg max-w-7xl mx-auto space-y-6">
  <div className="prose prose-invert prose-sm max-w-none text-lighterBeige">
    <h5>
        in high school, i would bike through the forest every day to school instead of taking the bus like everyone else.

        i memorized the huge network of trails by heart and can easily get from point a to point b, even in complete darkness.

        i often traverse the routes at midnight, and even when the snow was as high as my knees. there were occasionally coyote sightings that closed the forest entry off, but i enter anyways.
        
        to me, this was freedom.

        cool project ideas like <Link href="../phuture" className="a">phuture</Link> were inspired by spending time in nature and exploring hidden paths.
    </h5>

    {images.map((f, i) => (
      <Image
        key={f}
        src={`/notes/forest/${f}.jpg`}
        alt="forest"
        width={1200}
        height={800}
        className="w-full h-auto object-cover rounded mt-1 not-prose"
        sizes="(max-width: 768px) 100vw, 1280px"
        quality={i === 0 ? 85 : 75}
        loading={i === 0 ? "eager" : "lazy"}
        decoding="async"
      />
    ))}
  </div>
</div>