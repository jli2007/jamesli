import Link from "next/link";
import Image from "next/image";

export const images = ["f3","f1","f2","f0","f5","f6","f7","f8","f10","f11","f12","f13","f14"];

<div className="bg-black p-1 rounded-2xl shadow-lg max-w-7xl mx-auto space-y-6">
  <div className="prose prose-invert prose-sm max-w-none text-lighterBeige">
    <h5>
        today, i entered the vast forest behind my house for the first time since i started uwaterloo. and the strange thing was that i didn't feel the magic that i once did. 
        
        i can't shake that feeling. something that used to mean so much to me now feels empty, like a piece of me was left in the past.

        in high school, i would bike through the forest to school instead of taking the bus like everyone else. every morning. it was just me, the morning dew, and nature's voices.

        i memorized the huge network of trails by heart and could get from point a to point b even in complete darkness. i traversed the routes at midnight, and when the snow was as high as my knees.
        there were occasionally coyote sightings that closed off the entrance, but i entered anyways.
        
        to me, this was freedom. it was honestly a core part of my life during this time. something that differentiated me.

        i think this is symbolic of life's chapters. will always visit the forest when i'm home, but it's time to move on.
    </h5>

    <br/>

    {images.map((f, i) => (
      <Image
        key={f}
        src={`/notes/forest/${f}.jpg`}
        alt="forest"
        width={1200}
        height={800}
        className="w-full h-auto object-cover rounded mt-1 not-prose"
        sizes="(max-width: 768px) 100vw, 1280px"
        quality={i === 0 ? 85 : 75}
        loading={i === 0 ? "eager" : "lazy"}
        decoding="async"
      />
    ))}
  </div>
</div>