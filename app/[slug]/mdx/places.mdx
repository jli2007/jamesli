import { places } from "../places/places.ts";
import InteractivePlace from "../places/PlaceRender.tsx";

<div className="flex flex-col">
  <h1 className="text-xl font-extrabold text-lightBeige mb-0!">
    limitless.
  </h1>

  <h4 className="my-0!">
    i used to play geoguessr for hours every day during covid. 
    
    through streetview, i developed a fascination with the world around us. i don't want to live one-dimensionally: <span className="italic">not knowing what exists beyond the scope of the life i happened to grow up in.</span>

    curiosity, for me, is a refusal to be bounded by circumstance. it's an imagination beyond the small slice of life i was handed by default. i will travel the world one day.
    
    here, i hope to add locations that makes you wonder,
    <span className="text-lightBeige">
      " how would life look if i spawned here? "
    </span>
  </h4>

  <br/>

  <ScrollButtonTop>⬇ scroll to bottom</ScrollButtonTop>

  <br/>

  <div>
    {places.map((place, index) => (
      <InteractivePlace key={index} place={place} isLast={index === places.length - 1} />
    ))}
  </div>

  <ScrollButtonBottom>⬆ scroll to top</ScrollButtonBottom>
</div>