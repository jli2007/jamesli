import Image from "next/image"

export const videos = ["f9","f8","f7","f6","f5","f4","f3","f2","f1"];

<h4>you found a hidden page</h4>

<br/>

<h4>click to play/pause</h4>

<Image
  src="/notes/footy/f.png"
  alt="footy"
  width={1200}
  height={800}
  loading="eager"
  className="w-full"
  sizes="(max-width: 768px) 100vw, 1280px"
  quality={85}
/>

{videos.map((v) => (
  <div key={v}>
    <video
      autoPlay
      loop
      muted
      playsInline
      preload="none"
      className="w-full cursor-pointer"
      onClick={(e) => {
        e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
      }}
    >
      <source src={`../../../notes/footy/${v}.mp4`} type="video/mp4" />
      your browser does not support the video tag.
    </video>
  </div>
))}