<h4>2025 high school footy season. no volume, click to play/pause.</h4>

<img src="../../../notes/footy/f3.JPG" className="w-[100%]"></img>

<div>
  <h4>foul baiting 😭</h4>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy09.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy08.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy07.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy06.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy05.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy04.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy03.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy02.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<div>
  <video
    autoPlay
    loop
    muted
    playsInline
    className="w-full cursor-pointer"
    onClick={(e) => {
      e.currentTarget.paused ? e.currentTarget.play() : e.currentTarget.pause();
    }}
  >
    <source src="../../../notes/footy/footy01.mp4" type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</div>

<footer className="pt-4 border-t border-white/20 text-xs text-white/70 flex flex-row items-center">
  <span className="shiny-green-dot inline-block mr-2" />
  Last updated: 5/20/2025
</footer>