import { places } from "../places/places.ts";
import PlaceLikes from "../places/Likes.tsx";

<h3> google maps is incredible </h3>

<h4 className="description w-[90vw]">
  i used to play geoguessr hours on end every day during the lockdown. i
  developed a fascination with the world around us and still wander google maps
  occasionally in my free time. i will add interesting locations & pictures i
  stumble across here [fully random], with a focus on{" "}
  <span className="underline">variety</span> and lesser-known places to the
  public eye.{" "}
</h4>

#

<div className="flex items-baseline gap-[20px]">
  <h4 className="text-xl italic leading-none m-0">** interactive maps **</h4>
  <h6 className="leading-none m-0">(iframe is powerful wow)</h6>
</div>

<ScrollButtonTop>⬇ scroll to bottom</ScrollButtonTop>
<h4>
  want to add a location? send an email to{" "}
  <span className="underline">hello@jame.li</span>
</h4>
<hr />

 <div>
  {places.map((place, index) => (
    <div key={index} className="flex flex-col w-[90vw] allContainer">
      <div className="flex justify-between items-baseline pb-[10px] topContainer">
        <div className="flex items-center m-0 gap-[10px]">
          <h3 className="title !m-0 leading-none">{place.title}</h3>
          <span className="text-[40px] !m-0 leading-none">{place.flag}</span>
        </div>
        <div className="flex items-center gap-[10px] tagContainer m-0">
        {place.tags.map((tag, index) => {
            let shadowColor;
            if (tag.color === 'mediumseagreen') shadowColor = 'rgba(60,179,113,0.7)';
            else if (tag.color === 'orchid') shadowColor = 'rgba(218,112,214,0.7)';
            else if (tag.color === 'dodgerblue') shadowColor = 'rgba(30,144,255,0.7)';
            else if (tag.color === 'red') shadowColor = 'rgba(255,0,0,0.7)';
            else if (tag.color === 'darkSalmon') shadowColor = 'rgba(233,150,122,0.7)';
            else if (tag.color === 'slategray') shadowColor = 'rgba(112,128,144,0.7)';
            else if (tag.color === 'darkgoldenrod') shadowColor = 'rgba(184,134,11,0.7)';
            else if (tag.color === 'darkturquoise') shadowColor = 'rgba(0,206,209,0.7)';
            else if (tag.color === 'midnightblue') shadowColor = 'rgba(25,25,112,0.7)';
            else if (tag.color === 'mediumvioletred') shadowColor = 'rgba(199,21,133,0.7)';
            else if (tag.color === 'coral') shadowColor = 'rgba(255,127,80,0.7)';
            else shadowColor = 'rgba(0,0,0,0.3)';
          return(
          <div
            key={index}
            className={`text-white px-[15px] py-[2px] rounded-[15px] text-[12px]`}
            style={{ 
              backgroundColor: tag.color, 
              boxShadow: `0px 4px 8px ${shadowColor}`, 
            }}
          >
            <span>{tag.text}</span>
          </div>)
        })}
          {/* <PlaceLikes/> */}
        </div>
      </div>
      <iframe
        src={place.iframeSrc}
        className="w-[90vw] h-[75vh] border-0 iframe"
        allowFullScreen=""
        loading="lazy"
        referrerPolicy="no-referrer-when-downgrade"
      ></iframe>
      <h4 className="mt-[10px] mb-[25px] w-[90vw]">
        {place.description}
      </h4>
      <hr className="m-0 p-0 mb-[12px]" />
    </div>
  ))}
</div>

{/* break */}
<ScrollButtonBottom>⬆ scroll to top</ScrollButtonBottom>
# ...
<div className="mt-[10px]" />
